<h1>Shorten a URL</h1>

<%= form_for(@shortener) do |f| %>
  <%= render 'shared/error_messages' %>
  <div class="field">
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit "Shorten" %>
  </div>
<% end %>

